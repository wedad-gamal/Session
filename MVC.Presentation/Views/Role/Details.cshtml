@model RoleViewModel


<h1>Details @Model.Name</h1>

<div>
    @{
        await Html.RenderPartialAsync("_DetailsPartialView");
    }
    <div class="mt-3">
        <a class="btn btn-secondary" asp-action="Index">Back</a>
        <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</div>

